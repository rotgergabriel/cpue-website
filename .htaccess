# 1. Activar el motor de reescritura
RewriteEngine On

# IMPORTANTE: Indica la carpeta base si estás en localhost
RewriteBase /cpue-website/

# 2. Eliminar la extensión .php de la URL
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.php -f
RewriteRule ^([^\.]+)$ $1.php [NC,L]

# 3. Forzar HTTPS (DESACTIVADO PARA LOCALHOST)
# Solo activa esto cuando subas el sitio a un servidor real con SSL.
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# 4. Evitar que se listen los directorios
Options -Indexes

# 5. Proteger el propio archivo .htaccess
<Files .htaccess>
    Require all denied
</Files>