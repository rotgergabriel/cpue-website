# ----------------------------------------------------------------------
# .htaccess della ROOT (Cartella Principale)
# ----------------------------------------------------------------------

RewriteEngine On

# =============================================================
# CONFIGURAZIONE AMBIENTE
# Scegli la coppia di righe giusta (XAMPP o ONLINE)
# =============================================================

# --- CASO A: XAMPP (LOCALE) ---
# Usa queste righe ora:
RewriteBase /cpue-website/
RewriteCond %{REQUEST_URI} !^/cpue-website/public/

# --- CASO B: ONLINE (PRODUZIONE) ---
# Quando sarai su cpue.it, usa queste (togli i # e mettili sopra):
# RewriteBase /
# RewriteCond %{REQUEST_URI} !^/public/

# =============================================================
# REGOLE DI REINDIRIZZAMENTO
# =============================================================

# Se sei nella root vuota, vai a public
RewriteRule ^$ public/ [L]

# Se chiedi qualsiasi altra cosa, vai a public
RewriteRule ^(.*)$ public/$1 [L]

Options -Indexes