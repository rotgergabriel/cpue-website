# ----------------------------------------------------------------------
# .htaccess de la RAÍZ (Raíz del proyecto)
# ----------------------------------------------------------------------

# 1. Activar el motor de reescritura
RewriteEngine On

# 2. Definir la base (ajustar si la carpeta del proyecto cambia)
RewriteBase /cpue-website/

# 3. Redirigir la raíz directamente a la carpeta public
RewriteRule ^$ public/ [L]

# 4. Redirigir cualquier otra petición a la carpeta public
# (Solo si no empieza ya con /public/)
RewriteCond %{REQUEST_URI} !^/cpue-website/public/
RewriteRule ^(.*)$ public/$1 [L]

# 5. Seguridad: Evitar que se listen los directorios del proyecto
Options -Indexes